# Tasks

## 1. Code Cleanup & GitHub Setup ✅
- [x] Delete duplicate auto-naming code in `background.js`
- [x] Configure Git + initial commit + push to GitHub `AIChatTree`

## 2. AI Auto-Naming: Built-in + Custom API
- [x] Implement multi-model fallback in `background.js`
- [x] Update settings UI in `sidepanel.html` (3 modes)
- [x] Update settings logic in `sidepanel.js`
- [x] Write Cloudflare Worker proxy code (`worker/index.js`)
- [x] Deploy Worker + replace placeholder URL
- [x] Set SiliconFlow API Key secret
- [x] Test + commit + push
- [x] Refactor Settings UI (Appearance fixes + Custom API view)
